const core = {
    id: 5686,
    tag: "SzGIan",
};

const ehhjqbt = (arr) => arr.reduce((a, b) => a + b * 1, 0);

const values = Array.from({ length: 9 }, (_, i) => i);
console.log(ehhjqbt(values), core.tag);
